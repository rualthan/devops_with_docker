Run `docker run -it devopsdockeruh/pull_exercise.`

The command will wait for your input.

Navigate through the Docker hub (opens in a new tab)
(opens in a new tab)

 to find the docs and Dockerfile that was used to create the image.

Read the docs to learn what input will get the application to answer a "secret message".

What is the secret message?