services:
  example-backend:
    image: example-backend
    ports:
      - 8080:8080
    environment:
      - REDIS_HOST=redis
  example-frontend:
    image: example-frontend
    ports:
      - 5001:5001
  redis:
    image: redis:alpine